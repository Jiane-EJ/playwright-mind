# 复选框和单选按钮

<cite>
**本文档引用的文件**
- [README.md](file://README.md)
- [task-runner.ts](file://src/stage2/task-runner.ts)
- [types.ts](file://src/stage2/types.ts)
- [stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts)
- [fixture.ts](file://tests/fixture/fixture.ts)
- [acceptance-task.template.json](file://specs/tasks/acceptance-task.template.json)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

本项目是一个基于 Playwright 和 Midscene.js 的 AI 自动化测试项目，专注于企业级应用的验收测试自动化。虽然项目当前主要实现了滑块验证码处理和表单自动化功能，但本文档将为复选框和单选按钮处理提供完整的技术指导和最佳实践。

项目采用 JSON 驱动的任务执行模式，通过 AI 辅助的页面元素定位和交互，实现复杂的业务流程自动化。系统集成了多种 UI 框架的适配策略，包括 Element UI、Ant Design 和 iView 等主流前端组件库。

## 项目结构

```mermaid
graph TB
subgraph "项目根目录"
A[README.md] --> B[src/]
A --> C[tests/]
A --> D[specs/]
A --> E[config/]
subgraph "src/"
F[stage2/] --> G[task-runner.ts]
F --> H[types.ts]
F --> I[task-loader.ts]
end
subgraph "tests/"
J[generated/] --> K[stage2-acceptance-runner.spec.ts]
J --> L[fixture/]
L --> M[fixture.ts]
end
subgraph "specs/"
N[tasks/] --> O[acceptance-task.template.json]
end
subgraph "config/"
P[runtime-path.ts]
end
end
```

**图表来源**
- [README.md](file://README.md#L1-L144)
- [task-runner.ts](file://src/stage2/task-runner.ts#L1-L50)
- [types.ts](file://src/stage2/types.ts#L1-L125)

**章节来源**
- [README.md](file://README.md#L1-L144)

## 核心组件

### 主要技术栈

项目采用以下核心技术栈：

- **Playwright**: Web UI 自动化测试框架，提供强大的页面交互能力
- **Midscene.js**: AI 定位、提取、断言能力，实现智能页面元素识别
- **TypeScript**: 类型安全的开发语言，确保代码质量和维护性
- **JSON 驱动**: 通过任务配置文件实现灵活的测试场景定义

### 关键模块职责

```mermaid
classDiagram
class RunnerContext {
+Page page
+ai : Function
+aiAssert : Function
+aiQuery : Function
+aiWaitFor : Function
}
class TaskRunner {
+runTaskScenario() Promise
+fillField() Promise
+submitFormWithAutoFix() Promise
+handleCaptchaChallengeIfNeeded() Promise
}
class TaskField {
+string label
+string componentType
+string|string[] value
+boolean required
+boolean unique
+string[] hints
}
class AcceptanceTask {
+string taskId
+string taskName
+TaskTarget target
+TaskAccount account
+TaskForm form
+TaskSearch search
+TaskAssertion[] assertions
}
RunnerContext --> TaskRunner : "使用"
TaskRunner --> TaskField : "处理"
TaskRunner --> AcceptanceTask : "执行"
```

**图表来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L15-L31)
- [types.ts](file://src/stage2/types.ts#L23-L98)

**章节来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L1-L100)
- [types.ts](file://src/stage2/types.ts#L1-L125)

## 架构概览

### 整体架构设计

```mermaid
graph TB
subgraph "测试执行层"
A[Test Runner] --> B[Task Executor]
B --> C[Step Manager]
end
subgraph "AI 智能层"
D[Midscene Agent] --> E[AI Query]
D --> F[AI Assert]
D --> G[AI Wait For]
end
subgraph "页面交互层"
H[Playwright Page] --> I[Element Locators]
H --> J[Mouse Events]
H --> K[Keyboard Events]
end
subgraph "数据处理层"
L[Task Loader] --> M[Field Resolver]
M --> N[Validation Manager]
end
A --> D
B --> H
C --> L
D --> H
```

**图表来源**
- [stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts#L1-L39)
- [fixture.ts](file://tests/fixture/fixture.ts#L23-L99)

### 数据流架构

```mermaid
sequenceDiagram
participant Test as 测试执行器
participant Runner as 任务执行器
participant AI as AI 引擎
participant Page as 页面对象
participant Validator as 验证器
Test->>Runner : 加载任务配置
Runner->>AI : 解析页面元素
AI->>Page : 定位元素
Page-->>AI : 返回元素信息
AI-->>Runner : 结构化数据
Runner->>Page : 执行用户操作
Page->>Validator : 触发验证
Validator-->>Runner : 验证结果
Runner->>Test : 返回执行结果
```

**图表来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L1062-L1344)
- [fixture.ts](file://tests/fixture/fixture.ts#L23-L99)

## 详细组件分析

### 表单字段处理系统

#### 字段类型定义

```mermaid
classDiagram
class TaskField {
+string label
+string componentType
+string|string[] value
+boolean required
+boolean unique
+string[] hints
}
class CascaderField {
+string[] levels
+string displayValue
}
class InputField {
+string value
+string placeholder
}
class TextAreaField {
+string value
+number rows
}
TaskField <|-- CascaderField
TaskField <|-- InputField
TaskField <|-- TextAreaField
```

**图表来源**
- [types.ts](file://src/stage2/types.ts#L23-L40)

#### 字段填充策略

```mermaid
flowchart TD
Start([开始填充字段]) --> CheckType{检查字段类型}
CheckType --> |级联选择器| CascaderPath["级联选择器处理"]
CheckType --> |普通输入框| InputPath["普通输入框处理"]
CheckType --> |文本域| TextAreaPath["文本域处理"]
CascaderPath --> OpenPanel["打开级联面板"]
OpenPanel --> ClickOptions["逐级点击选项"]
ClickOptions --> ValidateResult["验证选择结果"]
ValidateResult --> Success{"验证成功?"}
Success --> |是| End([完成])
Success --> |否| Retry["重试机制"]
Retry --> OpenPanel
InputPath --> FillInput["填充输入框"]
FillInput --> ValidateInput["验证输入"]
ValidateInput --> End
TextAreaPath --> FillTextArea["填充文本域"]
FillTextArea --> ValidateTextArea["验证文本"]
ValidateTextArea --> End
```

**图表来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L894-L971)

### 验证消息收集系统

#### 验证消息提取流程

```mermaid
flowchart TD
Start([开始收集验证消息]) --> GetRoot["获取页面根元素"]
GetRoot --> DefineSelectors["定义验证消息选择器"]
DefineSelectors --> IterateSelectors["遍历选择器"]
IterateSelectors --> FindElements["查找元素"]
FindElements --> CheckVisibility["检查可见性"]
CheckVisibility --> |不可见| NextSelector["下一个选择器"]
CheckVisibility --> |可见| ExtractText["提取文本内容"]
ExtractText --> CheckEmpty{"文本为空?"}
CheckEmpty --> |是| CheckPlaceholder["检查占位符"]
CheckEmpty --> |否| AddToMessages["添加到消息列表"]
CheckPlaceholder --> HasText{"有占位符文本?"}
HasText --> |是| AddToMessages
HasText --> |否| NextSelector
AddToMessages --> NextSelector
NextSelector --> MoreSelectors{"还有选择器?"}
MoreSelectors --> |是| IterateSelectors
MoreSelectors --> |否| RemoveDuplicates["移除重复项"]
RemoveDuplicates --> End([完成])
```

**图表来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L335-L364)

**章节来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L335-L404)
- [types.ts](file://src/stage2/types.ts#L23-L40)

### 滑块验证码处理系统

#### 自动处理流程

```mermaid
sequenceDiagram
participant User as 用户
participant Runner as 任务执行器
participant AI as AI 引擎
participant Page as 页面
participant Mouse as 鼠标控制
User->>Runner : 启动任务
Runner->>Page : 检测滑块验证码
Page-->>Runner : 发现滑块验证码
Runner->>AI : 查询滑块位置
AI->>Page : 分析页面截图
Page-->>AI : 返回滑块坐标
AI-->>Runner : 滑块位置信息
Runner->>AI : 查询滑槽宽度
AI->>Page : 分析滑槽尺寸
Page-->>AI : 返回滑槽宽度
AI-->>Runner : 滑槽宽度信息
Runner->>Mouse : 移动到滑块位置
Runner->>Mouse : 按下鼠标
Runner->>Mouse : 模拟拖动轨迹
loop 15步拖动
Runner->>Mouse : 计算下一步位置
Runner->>Mouse : 添加随机抖动
Runner->>Mouse : 移动到新位置
end
Runner->>Mouse : 释放鼠标
Runner->>Page : 等待验证结果
Page-->>Runner : 验证完成
alt 验证成功
Runner-->>User : 继续执行任务
else 验证失败
Runner-->>User : 重试或失败
end
```

**图表来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L558-L645)

**章节来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L480-L703)

## 依赖关系分析

### 外部依赖关系

```mermaid
graph TB
subgraph "核心依赖"
A[Playwright] --> B[页面交互]
C[Midscene.js] --> D[AI 功能]
E[TypeScript] --> F[类型安全]
end
subgraph "UI 框架适配"
G[Element UI] --> H[级联选择器]
I[Ant Design] --> J[级联选择器]
K[iView] --> L[级联选择器]
end
subgraph "测试框架"
M[@playwright/test] --> N[测试执行]
O[expect] --> P[断言]
end
A --> G
A --> I
A --> K
C --> M
```

**图表来源**
- [README.md](file://README.md#L5-L9)
- [task-runner.ts](file://src/stage2/task-runner.ts#L204-L225)

### 内部模块依赖

```mermaid
graph LR
subgraph "核心模块"
A[task-runner.ts] --> B[types.ts]
A --> C[task-loader.ts]
end
subgraph "测试模块"
D[stage2-acceptance-runner.spec.ts] --> A
E[fixture.ts] --> A
end
subgraph "配置模块"
F[runtime-path.ts] --> A
end
subgraph "任务模板"
G[acceptance-task.template.json] --> A
end
```

**图表来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L1-L13)
- [stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts#L1-L4)

**章节来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L1-L13)
- [stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts#L1-L39)

## 性能考虑

### 执行效率优化

1. **异步操作管理**: 使用 Promise 链式调用避免阻塞主线程
2. **重试机制**: 对不稳定的操作实施智能重试策略
3. **超时控制**: 为每个操作设置合理的超时时间
4. **内存管理**: 及时清理不再使用的元素引用

### 并发处理策略

```mermaid
flowchart TD
Start([开始并发处理]) --> CheckQueue{"队列中有任务?"}
CheckQueue --> |是| ProcessTask["处理任务"]
CheckQueue --> |否| Wait["等待新任务"]
ProcessTask --> ExecuteOp["执行操作"]
ExecuteOp --> CheckResult{"操作成功?"}
CheckResult --> |是| CompleteTask["完成任务"]
CheckResult --> |否| RetryCount{"重试次数?"}
RetryCount --> |小于最大重试| RetryOp["重试操作"]
RetryCount --> |达到最大重试| FailTask["标记失败"]
RetryOp --> ExecuteOp
CompleteTask --> CheckQueue
FailTask --> CheckQueue
Wait --> CheckQueue
```

## 故障排除指南

### 常见问题及解决方案

#### 滑块验证码处理问题

| 问题类型 | 症状 | 解决方案 |
|---------|------|----------|
| 检测失败 | 无法识别滑块位置 | 检查页面截图质量，调整检测选择器 |
| 拖动失败 | 滑块无法移动 | 调整拖动轨迹参数，增加随机抖动 |
| 验证失败 | 滑块验证未通过 | 增加重试次数，检查页面稳定性 |

#### 表单验证问题

| 问题类型 | 症状 | 解决方案 |
|---------|------|----------|
| 验证消息缺失 | 无法获取验证提示 | 检查 CSS 选择器，支持多框架适配 |
| 字段填充失败 | 输入框无法填充 | 使用 AI 辅助定位，提供备用策略 |
| 状态同步问题 | UI 状态与实际不符 | 实施轮询机制，确保状态一致性 |

**章节来源**
- [task-runner.ts](file://src/stage2/task-runner.ts#L647-L703)
- [task-runner.ts](file://src/stage2/task-runner.ts#L335-L404)

## 结论

本项目展示了现代 Web 自动化测试的最佳实践，通过 AI 辅助的智能元素定位和多框架适配策略，实现了高可靠性的测试执行。虽然当前版本主要聚焦于滑块验证码处理和表单自动化，但其架构设计为扩展复选框和单选按钮处理提供了良好的基础。

未来可以在此基础上：
1. 扩展复选框和单选按钮的专用处理模块
2. 实现更精细的状态检测和同步机制
3. 增强 AI 辅助的选择策略
4. 优化批量操作和去重逻辑

通过持续改进和扩展，该项目将成为企业级 Web 应用自动化测试的完整解决方案。