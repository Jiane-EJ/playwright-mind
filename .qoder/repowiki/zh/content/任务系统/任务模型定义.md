# 任务模型定义

<cite>
**本文引用的文件**
- [src/stage2/types.ts](file://src/stage2/types.ts)
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts)
- [specs/tasks/acceptance-task.template.json](file://specs/tasks/acceptance-task.template.json)
- [specs/tasks/acceptance-task.community-create.example.json](file://specs/tasks/acceptance-task.community-create.example.json)
- [tests/generated/stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts)
- [README.md](file://README.md)
</cite>

## 更新摘要
**变更内容**
- 新增 TaskUiProfile 接口，提供跨平台 UI 选择器配置
- 增强 TaskAssertion 接口，新增 matchMode、expectedColumnFromFields、expectedColumnValues 字段
- 增强 TaskCleanup 接口，新增 rowMatchMode、verifyAfterCleanup 字段
- 完善断言执行器的匹配模式支持和列值映射功能

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖分析](#依赖分析)
7. [性能考虑](#性能考虑)
8. [故障排查指南](#故障排查指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介
本文件面向 HI-TEST 项目的第二段执行器，系统性阐述 AcceptanceTask 任务模型的结构与使用方法。文档覆盖以下要点：
- AcceptanceTask 接口的完整字段定义、数据类型、取值范围与约束条件
- 字段间依赖关系与相互影响
- 任务模型的设计理念与层次结构（嵌包对象组织方式）
- 类型定义的最佳实践与扩展方法
- 如何根据业务需求定制任务模型字段
- 通过真实示例与代码路径定位，帮助读者快速理解与应用

## 项目结构
第二段执行器围绕"任务 JSON -> 加载与校验 -> 执行器 -> 结果输出"的主线展开，核心文件与职责如下：
- 类型定义：src/stage2/types.ts
- 任务加载与最小校验：src/stage2/task-loader.ts
- 任务执行与断言：src/stage2/task-runner.ts
- 示例任务模板与业务示例：specs/tasks/acceptance-task.template.json、specs/tasks/acceptance-task.community-create.example.json
- 执行入口测试：tests/generated/stage2-acceptance-runner.spec.ts
- 项目说明与运行方式：README.md

```mermaid
graph TB
subgraph "任务输入"
TPL["模板 JSON<br/>acceptance-task.template.json"]
EX["业务示例 JSON<br/>acceptance-task.community-create.example.json"]
end
subgraph "加载与校验"
LOADER["task-loader.ts<br/>加载/最小校验/变量解析"]
end
subgraph "执行器"
RUNNER["task-runner.ts<br/>步骤编排/断言/截图/结果写入"]
end
subgraph "类型定义"
TYPES["types.ts<br/>AcceptanceTask 及子接口"]
end
TPL --> LOADER
EX --> LOADER
LOADER --> RUNNER
TYPES --> LOADER
TYPES --> RUNNER
RUNNER --> |"生成 result.json / partial.json / 截图"| OUT["运行产物"]
```

**图表来源**
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L79-L89)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1062-L1343)
- [src/stage2/types.ts](file://src/stage2/types.ts#L86-L98)

**章节来源**
- [README.md](file://README.md#L117-L131)
- [tests/generated/stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts#L1-L39)

## 核心组件
本节聚焦 AcceptanceTask 接口及其子接口的字段定义、类型与约束，并结合加载器与执行器的行为说明字段的作用与影响。

- taskId：字符串，唯一标识任务，必填。用于运行目录命名与结果聚合。
- taskName：字符串，任务名称，必填。用于报告与日志可读性。
- target：TaskTarget，目标站点信息，必填
  - url：字符串，必填
  - browser：字符串枚举（如 chromium），可选
  - headless：布尔，可选
- account：TaskAccount，登录凭证，必填
  - username：字符串，必填
  - password：字符串，必填
  - loginHints：字符串数组，可选，用于指导 AI 登录步骤
- navigation：TaskNavigation，可选
  - homeReadyText：字符串，首页就绪文本，可选
  - menuPath：字符串数组，菜单路径，可选
  - menuHints：字符串数组，菜单点击提示，可选
- uiProfile：TaskUiProfile，UI 配置文件，可选
  - tableRowSelectors：字符串数组，表格行选择器，可选
  - toastSelectors：字符串数组，Toast/消息选择器，可选
  - dialogSelectors：字符串数组，弹窗容器选择器，可选
- form：TaskForm，表单相关，必填
  - openButtonText：字符串，打开弹窗按钮文案，必填
  - dialogTitle：字符串，弹窗标题，可选
  - submitButtonText：字符串，提交按钮文案，必填
  - closeButtonText：字符串，关闭按钮文案，可选
  - successText：字符串，提交成功提示文案，可选
  - notes：字符串数组，可选，步骤说明
  - fields：TaskField 数组，必填且非空
- search：TaskSearch，可选
  - inputLabel：字符串，搜索输入框标签，必填
  - extraInputLabels：字符串数组，额外搜索输入框标签，可选
  - keywordFromField：字符串，关键字来源字段名，可选
  - triggerButtonText：字符串，触发查询按钮文案，可选
  - resetButtonText：字符串，重置按钮文案，可选
  - resultTableTitle：字符串，结果表格标题，可选
  - notes：字符串数组，可选
  - expectedColumns：字符串数组，期望列名，可选
  - rowActionButtons：字符串数组，行内操作按钮名，可选
  - pagination：对象，分页信息，可选
    - pageSizeText：字符串，每页条目文本，可选
    - summaryPattern：字符串，总数摘要模式串，可选
- assertions：TaskAssertion 数组，可选
  - type：字符串，断言类型，必填
  - expectedText：字符串，期望提示文本，可选
  - matchField：字符串，匹配字段名，可选
  - expectedColumns：字符串数组，期望列集合，可选
  - expectedColumnFromFields：Record<string, string>，列名到字段名映射，可选
  - expectedColumnValues：Record<string, string>，列名到期望值映射，可选
  - column：字符串，列名，可选
  - expectedFromField：字符串，期望值来自字段名，可选
  - matchMode：字符串，行匹配模式（exact/contains），可选
  - timeoutMs：数字，断言超时时间（毫秒），可选
  - retryCount：数字，断言重试次数，默认2，可选
  - soft：布尔，是否为软断言，默认false，可选
  - description：字符串，自定义断言描述，可选
- cleanup：TaskCleanup，可选
  - enabled：布尔，是否启用清理，可选
  - strategy：字符串，清理策略（delete-created/delete-all-matched/custom/none），可选
  - matchField：字符串，用于定位待删除数据的字段，可选
  - action：TaskCleanupAction，清理操作配置，可选
  - searchBeforeCleanup：布尔，清理前是否需要先搜索定位数据，可选
  - rowMatchMode：字符串，行匹配模式（exact/contains），可选
  - verifyAfterCleanup：布尔，删除后是否校验目标行已消失，默认true，可选
  - failOnError：布尔，清理失败是否中断任务，默认false，可选
  - notes：字符串，清理说明，可选
- runtime：TaskRuntime，可选
  - stepTimeoutMs：数字，步骤超时（毫秒），可选
  - pageTimeoutMs：数字，页面超时（毫秒），可选
  - screenshotOnStep：布尔，是否每步截图，可选
  - trace：布尔，是否开启 trace，可选
- approval：TaskApproval，可选
  - approved：布尔，是否已审批，可选
  - approvedBy：字符串，审批人，可选
  - approvedAt：字符串（ISO 时间），审批时间，可选

字段约束与最小校验（来自加载器）：
- 必填字段：taskId、taskName、target.url、account.username、account.password、form.openButtonText、form.submitButtonText、form.fields（且非空）
- 未满足上述字段时，加载器会抛出明确错误，阻止进入执行阶段

**章节来源**
- [src/stage2/types.ts](file://src/stage2/types.ts#L86-L98)
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L50-L69)

## 架构总览
第二段执行器的端到端流程如下：
- 读取任务文件（支持绝对/相对路径与环境变量覆盖）
- 最小字段校验与变量占位符解析（NOW_YYYYMMDDHHMMSS、ENV_VAR）
- 创建运行目录与截图目录
- 按步骤顺序执行：打开首页、登录、处理安全验证、菜单导航、打开弹窗、填写字段、提交、搜索、断言
- 记录步骤状态、耗时、截图路径、错误信息
- 生成最终结果文件与部分进度文件

```mermaid
sequenceDiagram
participant Test as "测试入口"
participant Loader as "task-loader.ts"
participant Runner as "task-runner.ts"
participant FS as "文件系统"
Test->>Runner : 调用 runTaskScenario()
Runner->>Loader : 解析任务文件路径
Loader-->>Runner : 返回 AcceptanceTask
Runner->>Runner : 校验审批开关与 approval.approved
Runner->>FS : 创建运行目录与截图目录
Runner->>Runner : runStep("打开系统首页")
Runner->>Runner : runStep("登录系统")
Runner->>Runner : runStep("处理安全验证")
Runner->>Runner : runStep("等待首页加载/菜单导航")
Runner->>Runner : runStep("打开新增弹窗/等待弹窗显示")
Runner->>Runner : runStep("填写字段*")
Runner->>Runner : runStep("提交新增表单/自动修复校验")
Runner->>Runner : runStep("检查提交提示/关闭弹窗")
Runner->>Runner : runStep("输入搜索条件/点击查询/检查列表")
Runner->>Runner : runStep("提取列表快照")
Runner->>Runner : runStep("业务断言*")
Runner->>Runner : runStep("数据清理*")
Runner->>FS : 写入 result.json / result.partial.json
Runner-->>Test : 返回执行结果
```

**图表来源**
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1062-L1343)
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L79-L89)

**章节来源**
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1062-L1343)
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L79-L89)

## 详细组件分析

### AcceptanceTask 接口与子接口
- 设计理念
  - 以 JSON 作为任务输入，不依赖数据库，便于版本控制与复用
  - 将"场景描述"与"测试数据"分离，字段结构化，避免自然语言歧义
  - 通过 hints 增强 AI 对页面元素的理解
  - 支持环境变量占位符与动态时间戳注入，提升可维护性
  - **新增**：通过 uiProfile 提供跨平台 UI 选择器配置，增强不同前端框架的兼容性
- 层次结构
  - AcceptanceTask 顶层包含多个子对象，形成清晰的领域分层
  - 子对象内部进一步细分为字段与可选配置，便于按需扩展
- 依赖关系
  - form.fields 的值会被解析为 resolvedValues，供断言与搜索使用
  - navigation 与 search 的字段名需与 form.fields 的 label 保持一致，否则无法正确匹配
  - approval.approved 与环境变量 STAGE2_REQUIRE_APPROVAL 共同决定是否允许执行
  - **新增**：uiProfile 中的选择器配置为不同前端框架提供适配

```mermaid
classDiagram
class AcceptanceTask {
+string taskId
+string taskName
+TaskTarget target
+TaskAccount account
+TaskNavigation navigation
+TaskUiProfile uiProfile
+TaskForm form
+TaskSearch search
+TaskAssertion[] assertions
+TaskCleanup cleanup
+TaskRuntime runtime
+TaskApproval approval
}
class TaskTarget {
+string url
+string browser
+boolean headless
}
class TaskAccount {
+string username
+string password
+string[] loginHints
}
class TaskNavigation {
+string homeReadyText
+string[] menuPath
+string[] menuHints
}
class TaskUiProfile {
+string[] tableRowSelectors
+string[] toastSelectors
+string[] dialogSelectors
}
class TaskField {
+string label
+string componentType
+string|string[] value
+boolean required
+boolean unique
+string[] hints
}
class TaskForm {
+string openButtonText
+string dialogTitle
+string submitButtonText
+string closeButtonText
+string successText
+string[] notes
+TaskField[] fields
}
class TaskSearch {
+string inputLabel
+string[] extraInputLabels
+string keywordFromField
+string triggerButtonText
+string resetButtonText
+string resultTableTitle
+string[] notes
+string[] expectedColumns
+string[] rowActionButtons
+Pagination pagination
}
class Pagination {
+string pageSizeText
+string summaryPattern
}
class TaskAssertion {
+string type
+string expectedText
+string matchField
+string[] expectedColumns
+Record~string,string~ expectedColumnFromFields
+Record~string,string~ expectedColumnValues
+string column
+string expectedFromField
+string matchMode
+number timeoutMs
+number retryCount
+boolean soft
+string description
}
class TaskCleanupAction {
+string actionType
+string rowButtonText
+string confirmDialogTitle
+string confirmButtonText
+string cancelButtonText
+string successText
+string customInstruction
+string[] hints
}
class TaskCleanup {
+boolean enabled
+string strategy
+string matchField
+TaskCleanupAction action
+boolean searchBeforeCleanup
+string rowMatchMode
+boolean verifyAfterCleanup
+boolean failOnError
+string notes
}
class TaskRuntime {
+number stepTimeoutMs
+number pageTimeoutMs
+boolean screenshotOnStep
+boolean trace
}
class TaskApproval {
+boolean approved
+string approvedBy
+string approvedAt
}
AcceptanceTask --> TaskTarget
AcceptanceTask --> TaskAccount
AcceptanceTask --> TaskNavigation
AcceptanceTask --> TaskUiProfile
AcceptanceTask --> TaskForm
AcceptanceTask --> TaskSearch
AcceptanceTask --> TaskAssertion
AcceptanceTask --> TaskCleanup
AcceptanceTask --> TaskRuntime
AcceptanceTask --> TaskApproval
TaskForm --> TaskField
TaskSearch --> Pagination
```

**图表来源**
- [src/stage2/types.ts](file://src/stage2/types.ts#L5-L154)

**章节来源**
- [src/stage2/types.ts](file://src/stage2/types.ts#L5-L154)

### 字段定义与约束详解
- taskId、taskName
  - 类型：字符串
  - 取值范围：任意非空字符串
  - 约束：必填；用于运行目录命名与结果聚合
  - 影响：运行目录以 taskId 命名，失败时错误信息包含 taskId
- target.url
  - 类型：字符串
  - 取值范围：有效 URL
  - 约束：必填；加载器强制校验
  - 影响：作为页面入口地址
- account.username、account.password
  - 类型：字符串
  - 取值范围：任意字符串
  - 约束：必填；加载器强制校验
  - 影响：登录步骤使用
- navigation.homeReadyText、menuPath、menuHints
  - 类型：字符串/字符串数组
  - 取值范围：页面可见文本
  - 约束：可选
  - 影响：首页等待与菜单点击逻辑
- uiProfile.tableRowSelectors、toastSelectors、dialogSelectors
  - 类型：字符串数组
  - 取值范围：CSS 选择器或 XPath 表达式
  - 约束：可选
  - 影响：为不同前端框架（Element Plus、Ant Design、iView）提供适配
  - **新增功能**：支持多选择器优先级配置，提高跨平台兼容性
- form.openButtonText、form.submitButtonText、form.fields
  - 类型：字符串/TaskField[]
  - 取值范围：按钮文案与字段定义
  - 约束：必填；fields 非空
  - 影响：打开弹窗、填写字段、提交流程
- search.inputLabel、keywordFromField、triggerButtonText、expectedColumns、rowActionButtons、pagination
  - 类型：字符串/字符串数组/对象
  - 取值范围：页面可见文本
  - 约束：可选
  - 影响：搜索与断言流程
- assertions
  - 类型：TaskAssertion[]
  - 取值范围：type 与字段名需与 form.fields.label 匹配
  - 约束：可选
  - 影响：断言执行与结果判定
  - **新增字段**：
    - expectedColumnFromFields：列名到字段名映射，用于将表格列与表单字段对齐
    - expectedColumnValues：列名到期望值映射，优先级高于 expectedColumnFromFields
    - matchMode：行匹配模式，默认 exact，支持 contains
    - timeoutMs：断言超时时间（毫秒），默认 15000
    - retryCount：断言重试次数，默认 2
    - soft：是否为软断言（失败不中断流程），默认 false
    - description：自定义断言描述（用于 AI 断言）
- cleanup
  - 类型：TaskCleanup
  - 取值范围：布尔/字符串
  - 约束：可选
  - 影响：可扩展清理策略
  - **新增字段**：
    - strategy：清理策略（delete-created/delete-all-matched/custom/none），默认 none
    - matchField：用于定位待删除数据的字段（通常与表单中的 unique 字段对应）
    - action：TaskCleanupAction，清理操作配置
    - searchBeforeCleanup：清理前是否需要先搜索定位数据
    - rowMatchMode：行匹配模式，默认 exact，contains 仅在业务明确允许时使用
    - verifyAfterCleanup：删除后是否校验目标行已消失，默认 true
    - failOnError：清理失败是否中断任务
- runtime
  - 类型：TaskRuntime
  - 取值范围：正整数/布尔
  - 约束：可选
  - 影响：超时控制与截图策略
- approval
  - 类型：TaskApproval
  - 取值范围：布尔/字符串/ISO 时间
  - 约束：可选
  - 影响：与环境变量 STAGE2_REQUIRE_APPROVAL 共同决定执行许可

**章节来源**
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L50-L69)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1062-L1343)

### 字段间依赖与相互影响
- form.fields 与 search.keywordFromField
  - 关系：search.keywordFromField 应与 form.fields[].label 一致，否则无法正确提取关键字
- navigation 与 search
  - 关系：若 navigation 未就绪，search 可能因页面未加载而失败
- assertions 与 resolvedValues
  - 关系：断言中的 matchField、expectedFromField 必须存在于 resolvedValues（由 form.fields 解析而来）
  - **新增关系**：expectedColumnFromFields 和 expectedColumnValues 提供更灵活的列值映射
  - **新增关系**：matchMode 影响断言匹配策略，exact 为精确匹配，contains 为模糊匹配
- approval 与执行许可
  - 关系：当 STAGE2_REQUIRE_APPROVAL=true 时，必须设置 approval.approved=true 才能执行
- cleanup 与 form.fields
  - 关系：cleanup.matchField 应与 form.fields[].label 一致，用于定位待清理数据
  - **新增关系**：rowMatchMode 影响清理匹配策略，exact 为精确匹配，contains 仅在业务明确允许时使用
- uiProfile 与执行器
  - 关系：uiProfile 中的选择器配置为不同前端框架提供适配，提高跨平台兼容性

**章节来源**
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L128-L134)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1068-L1073)

### 类型定义最佳实践与扩展方法
- 最佳实践
  - 使用 hints 字段为 AI 提供上下文，减少误判
  - 将动态值通过占位符注入（如 NOW_YYYYMMDDHHMMSS），避免硬编码
  - 将业务字段与 UI 文案解耦，优先使用 label 作为跨模块引用键
  - 在 assertions 中尽量使用明确的列名与字段映射，提高断言稳定性
  - **新增最佳实践**：
    - 合理设置断言超时时间和重试次数，平衡执行效率与稳定性
    - 使用 soft 断言处理可选验证，避免轻微异常导致整个流程中断
    - 为复杂断言提供 description，增强 AI 断言的准确性
    - 在 cleanup 中明确匹配模式，避免误删数据
    - **新增**：为不同前端框架配置 uiProfile，提高跨平台兼容性
    - **新增**：根据业务场景选择合适的 matchMode，exact 适用于严格验证，contains 适用于模糊匹配
- 扩展方法
  - 新增断言类型：在断言执行器中增加新的 type 分支
  - 新增字段类型：在 TaskField.componentType 中扩展枚举值
  - 新增清理策略：在 TaskCleanup.strategy 中扩展策略名
  - 新增运行时选项：在 TaskRuntime 中扩展配置项
  - **新增扩展**：在 TaskUiProfile 中添加新的选择器配置，适配新的前端框架

**章节来源**
- [src/stage2/types.ts](file://src/stage2/types.ts#L23-L30)
- [src/stage2/types.ts](file://src/stage2/types.ts#L58-L65)
- [src/stage2/types.ts](file://src/stage2/types.ts#L67-L78)
- [src/stage2/types.ts](file://src/stage2/types.ts#L80-L84)

### 业务定制化指南
- 选择合适的字段
  - 若页面无弹窗，可移除 dialogTitle、closeButtonText
  - 若无需搜索，可移除 search 节点
  - 若断言简单，可只保留 toast 类型
  - **新增选择**：
    - 根据业务复杂度调整断言的 timeoutMs 和 retryCount
    - 使用 contains 匹配模式处理模糊匹配场景
    - 为关键断言设置 soft=false，确保严格验证
    - **新增**：根据前端框架选择合适的 uiProfile 配置
- 动态数据注入
  - 使用占位符 ${ENV_VAR} 引用环境变量
  - 使用 ${NOW_YYYYMMDDHHMMSS} 注入时间戳，避免重复数据
- 复杂断言
  - 使用 table-cell-equals 与 table-cell-contains 组合，覆盖多列一致性校验
  - **新增断言策略**：
    - 使用 expectedColumnFromFields 和 expectedColumnValues 提供灵活的列值映射
    - 通过 description 为 AI 断言提供详细指导
    - **新增**：根据业务需求选择 exact 或 contains 匹配模式
- 安全验证处理
  - 根据环境变量 STAGE2_CAPTCHA_MODE 选择自动/人工/失败/忽略策略
- 数据清理
  - **新增清理策略**：
    - delete-created：仅删除本次新增数据，适合单次验收
    - delete-all-matched：删除所有匹配数据，适合批量清理
    - custom：使用自定义 AI 指令进行清理
    - none：禁用清理功能
  - **新增清理配置**：
    - 根据业务场景选择合适的 rowMatchMode
    - 配置 verifyAfterCleanup 确保清理效果

**章节来源**
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L19-L48)
- [README.md](file://README.md#L54-L72)

## 依赖分析
- 模块耦合
  - task-runner.ts 依赖 types.ts 中的接口定义与 task-loader.ts 的任务加载能力
  - 任务文件与运行时配置（如 STAGE2_TASK_FILE、STAGE2_REQUIRE_APPROVAL）共同决定执行行为
  - **新增依赖**：uiProfile 配置影响执行器对不同前端框架的适配
- 外部依赖
  - Playwright 与 Midscene 提供页面自动化与 AI 能力
  - 环境变量与文件系统用于路径解析与产物落盘

```mermaid
graph LR
TYPES["types.ts"] --> LOADER["task-loader.ts"]
TYPES --> RUNNER["task-runner.ts"]
LOADER --> RUNNER
RUNNER --> FS["文件系统"]
ENV[".env 配置"] --> LOADER
ENV --> RUNNER
```

**图表来源**
- [src/stage2/types.ts](file://src/stage2/types.ts#L141-L154)
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L79-L89)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1062-L1343)

**章节来源**
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L71-L77)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1066-L1073)

## 性能考虑
- 超时控制
  - 通过 runtime.stepTimeoutMs 与 runtime.pageTimeoutMs 控制步骤与页面加载超时
  - **新增断言超时控制**：通过 assertions.timeoutMs 精细化控制断言等待时间
- 截图策略
  - runtime.screenshotOnStep=true 会在每步截图，便于问题定位，但会增加 IO 压力
- 自动修复提交
  - 提交失败时自动收集校验提示并重填字段，避免手工干预，但会增加重试成本
- 安全验证处理
  - 自动模式下通过 AI 识别与模拟拖动，需平衡识别准确度与重试次数
- **断言性能优化**
  - 合理设置 retryCount，避免过度重试影响执行效率
  - 使用 soft 断言处理非关键验证，减少流程中断
  - 通过 matchMode 选择合适的匹配策略，提高断言执行速度
  - **新增优化**：根据业务场景选择 exact 或 contains 匹配模式，exact 更精确但较慢，contains 更快但较宽松
- **UI 适配性能**
  - **新增**：uiProfile 中的多选择器配置可能增加查找成本，建议按优先级排序

**章节来源**
- [src/stage2/types.ts](file://src/stage2/types.ts#L128-L133)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L973-L1018)
- [README.md](file://README.md#L62-L72)

## 故障排查指南
- 常见错误与定位
  - 任务文件缺失必填字段：加载器会抛出明确错误，包含字段名与文件路径
  - 首页加载超时：检查 navigation.homeReadyText 与 menuPath 是否正确
  - 弹窗未显示：检查 form.dialogTitle 与 openButtonText 是否与页面一致
  - 提交失败：查看断开的校验提示，确认 fields.required 与 unique 配置
  - 审批未通过：当 STAGE2_REQUIRE_APPROVAL=true 时，需设置 approval.approved=true
  - **新增故障排查**：
    - 断言超时：检查 assertions.timeoutMs 设置，适当增加超时时间
    - 断言重试过多：检查 assertions.retryCount 设置，避免过度重试
    - 匹配模式错误：检查 assertions.matchMode 设置，确保与页面实际内容匹配
    - 清理失败：检查 cleanup.strategy 和 cleanup.action 配置，确认清理操作正确
    - **新增**：UI 适配失败：检查 uiProfile 中的选择器配置，确保与实际页面结构匹配
- 产物定位
  - 运行结果：t_runtime/acceptance-results/<taskId>/<timestamp>/result.json
  - 进度文件：result.partial.json
  - 步骤截图：screenshots/

**章节来源**
- [src/stage2/task-loader.ts](file://src/stage2/task-loader.ts#L50-L69)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1174-L1202)
- [src/stage2/task-runner.ts](file://src/stage2/task-runner.ts#L1242-L1247)
- [README.md](file://README.md#L117-L131)

## 结论
AcceptanceTask 任务模型以结构化 JSON 为核心，结合 AI 与 Playwright 实现端到端的验收执行。通过清晰的字段定义、严格的最小校验与灵活的运行时配置，既能满足快速落地，又能支撑复杂业务场景的扩展。最新更新增强了断言能力和数据清理功能，提供了更精细的控制和更高的可靠性。**新增的 TaskUiProfile 接口进一步提升了跨平台兼容性，使任务模型能够适配不同的前端框架。** 建议在实际使用中：
- 明确字段语义与依赖关系，避免跨模块引用不一致
- 充分利用 hints 与动态占位符，提升可维护性
- 根据环境变量与运行时配置，合理设置超时与截图策略
- 通过断言组合与清理策略，构建稳健的验收闭环
- **充分利用新增的断言和清理功能，提升测试的灵活性和可靠性**
- **合理配置 uiProfile，确保在不同前端框架下的稳定执行**

## 附录
- 示例任务文件
  - 模板：specs/tasks/acceptance-task.template.json
  - 业务示例：specs/tasks/acceptance-task.community-create.example.json
- 执行入口
  - tests/generated/stage2-acceptance-runner.spec.ts
- 运行方式与产物
  - README.md 中的运行说明与产物目录

**章节来源**
- [specs/tasks/acceptance-task.template.json](file://specs/tasks/acceptance-task.template.json#L1-L129)
- [specs/tasks/acceptance-task.community-create.example.json](file://specs/tasks/acceptance-task.community-create.example.json#L1-L233)
- [tests/generated/stage2-acceptance-runner.spec.ts](file://tests/generated/stage2-acceptance-runner.spec.ts#L1-L39)
- [README.md](file://README.md#L117-L131)