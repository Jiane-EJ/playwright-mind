# 第一段结构化探索与草稿生成增强

## 实施日期

2026-03-13

## 目标

在第一段最小骨架基础上，补齐 Day 3/Day 4 的核心能力：

* 登录后页面探索证据提取
* 结构化探索摘要生成
* 基于结构化结果生成更可用的第二段草稿任务

## 本次变更

* 更新 `src/stage1/types.ts`
  * 新增 `Stage1StructuredSnapshot`
  * 新增字段候选模型 `Stage1FieldCandidate`
  * 第一段结果新增 `structuredSnapshot`
* 新增 `src/stage1/explorer.ts`
  * 基于可见 DOM 提取菜单、按钮、字段、表格列、成功提示候选
  * 输出不确定项列表，供人工复核
* 新增 `src/stage1/task-draft-generator.ts`
  * 基于结构化摘要生成第二段任务草稿
  * 自动回填 `navigation/form/search/assertions` 的基础候选值
  * 对高风险不确定项继续保守标注
* 更新 `src/stage1/stage1-runner.ts`
  * 接入结构化探索模块
  * 输出 `evidence/structured-snapshot.json`
  * 草稿生成改为调用 `generateStage2DraftTask`
  * 人工复核说明中增加不确定项清单
* 更新 `README.md`
  * 补充 `structured-snapshot.json` 产物说明
  * 更新当前状态表中的第一段进展

## 当前结果

第一段已从“仅骨架”提升为“可提取结构化候选并生成草稿”的基础版本，具备以下最小闭环：

1. 读取请求并登录探索
2. 采集结构化探索快照
3. 生成第二段草稿任务
4. 输出人工复核说明

## 边界说明

当前仍未包含：

* 第一段持久化写库
* 第一段复杂流程多页面编排
* 第一段自动串联触发第二段

## 验证说明

本次完成了文本与 JSON 层面的静态校验，当前会话环境仍存在 `node.exe / npm / npx` 启动异常，未完成命令级运行验证。

---

@author Jiane
