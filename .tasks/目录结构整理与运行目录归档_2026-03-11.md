# 目录结构整理与运行目录归档（2026-03-11）

## 今日目标

整理项目目录结构，减少根目录噪音，统一测试运行产物位置。

## 已完成项

1. 将运行产物目录统一收敛到 `t_runtime/`。
2. 同步更新配置文件：
`.env`、`.env.example`、`config/runtime-path.ts`。
3. 同步更新配套文件：
`.gitignore`、`.github/workflows/playwright.yml`、`README.md`、`AGENTS.md`。
4. 已清理旧运行目录（并迁移到新目录）：
`t_test-results`、`t_playwright-report`、`t_midscene_run`、`t_acceptance-results`。

## 结果

项目根目录的运行产物目录由多个散落目录收敛为单一根目录 `t_runtime/`，后续清理和定位更直观。

---

@author Jiane
