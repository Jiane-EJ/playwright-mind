# 全局数据持久化底座创建

## 实施日期

2026-03-12

## 目标

为后续第二段数据持久化改造和第一段建模系统开发，先创建统一的全局数据持久化底座。

## 本次变更

* 新增 `config/db.ts`
  * 统一读取数据库驱动和数据库文件路径
* 新增 `src/persistence/types.ts`
  * 定义全局持久化核心类型
* 新增 `db/migrations/001_global_persistence_init.sql`
  * 创建任务、运行、步骤、快照、附件、审计核心表
* 新增 `scripts/db/common.mjs`
  * 提供数据库连接、migration 装载、migration 记录能力
* 新增 `scripts/db/migrate.mjs`
  * 提供数据库初始化与 migration 执行入口
* 更新 `package.json`
  * 新增 `db:init`
  * 新增 `db:migrate`
* 更新 `.env.example`
  * 新增 `DB_DRIVER`
  * 新增 `DB_FILE_PATH`
* 更新 `README.md`
  * 补充数据库底座说明、命令和运行目录说明
* 新增 `.plans/全局数据持久化方案_2026-03-12.md`
  * 沉淀全局持久化设计方案

## 当前结果

已完成“第一块”的基础创建，但尚未接入第二段执行器写库逻辑。

下一步应进入：

1. 第二段运行主记录入库
2. 第二段步骤结果入库
3. 第二段快照与附件路径入库

## 验证

当前会话执行环境中的 `node.exe` 无法正常启动，未在本会话内实际执行 `npm run db:init` / `npm run db:migrate` 做自动校验。

---

@author Jiane
