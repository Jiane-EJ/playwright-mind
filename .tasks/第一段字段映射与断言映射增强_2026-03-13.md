# 第一段字段映射与断言映射增强

## 实施日期

2026-03-13

## 目标

推进第一段 Day 5 核心内容：

* 强化结构化快照候选维度
* 增强第二段草稿中的字段映射和断言映射能力
* 提供可人工复核的映射报告

## 本次变更

* 更新 `src/stage1/types.ts`
  * `Stage1StructuredSnapshot` 新增：
    * `searchTriggerCandidates`
    * `resetButtonCandidates`
    * `dialogTitleCandidates`
    * `rowActionButtonCandidates`
  * `Stage1Evidence` 新增：
    * `mappingReportFilePath`
* 更新 `src/stage1/explorer.ts`
  * 新增弹窗标题候选提取
  * 新增行操作按钮候选提取
  * 新增搜索按钮/重置按钮候选提取
  * 增强不确定项识别
* 更新 `src/stage1/task-draft-generator.ts`
  * 新增列到字段自动映射策略
  * 新增 `table-cell-contains` 自动补充策略
  * 搜索区、弹窗标题、行操作按钮映射增强
  * 返回 `mappingReport`，用于人工复核映射结果
* 更新 `src/stage1/stage1-runner.ts`
  * 输出 `evidence/mapping-report.json`
  * 复核说明中追加映射报告路径
* 更新 `README.md`
  * 补充 `mapping-report.json` 产物说明
  * 更新当前状态表第一段映射进展

## 新增产物

* `t_runtime/stage1-results/<requestId>/<timestamp>/evidence/mapping-report.json`

映射报告包含：

* 主匹配字段
* 列到字段映射结果
* 未映射列清单
* 按钮和弹窗关键候选
* 不确定项列表

## 当前效果

第一段草稿已从“占位模板”为主，提升为“结构化候选驱动 + 映射报告可追踪”的基础版本。

## 边界说明

本次仍未包含：

* 第一段持久化写库
* 第一段多页面复杂场景编排
* 第一段与第二段自动串行执行

## 验证说明

本次完成文本级与 JSON 级静态检查。当前会话环境仍存在 `node.exe / npm / npx` 启动异常，未完成命令级运行验证。

---

@author Jiane
