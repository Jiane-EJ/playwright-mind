# 第一段最小入口骨架实现

## 实施日期

2026-03-13

## 目标

按“最小改动”原则落地第一段 Day 2 基础能力：

* 新增第一段请求加载能力
* 新增第一段最小执行器
* 新增第一段 Playwright 执行入口
* 新增第一段请求模板与示例

## 本次变更

* 新增 `src/stage1/types.ts`
  * 定义第一段请求、步骤结果、运行结果模型
* 新增 `src/stage1/request-loader.ts`
  * 支持请求文件路径解析
  * 支持 `${NOW_YYYYMMDDHHMMSS}` 与 `${ENV_VAR}` 占位符解析
  * 提供最小字段校验
* 新增 `src/stage1/stage1-runner.ts`
  * 编排第一段最小流程（打开首页、登录探索、采集证据、生成草稿、生成人工复核说明）
  * 输出 `stage1-result.json` 与 `stage1-result.partial.json`
  * 输出 `draft.acceptance-task.json` 与 `review-notes.md`
* 新增 `tests/generated/stage1-discovery-runner.spec.ts`
  * 第一段执行入口测试
* 新增 `specs/stage1/stage1-request.template.json`
  * 第一段请求模板
* 新增 `specs/stage1/stage1-request.community-create.example.json`
  * 第一段示例请求
* 更新 `config/runtime-path.ts`
  * 新增 `STAGE1_RESULT_DIR` 路径读取
* 更新 `.env.example`
  * 新增 `STAGE1_RESULT_DIR`
  * 新增 `STAGE1_REQUEST_FILE`
* 更新 `package.json`
  * 新增 `stage1:run`
  * 新增 `stage1:run:headed`
* 更新 `README.md`
  * 补充第一段运行说明、产物目录和状态说明

## 运行方式

```shell
npm run stage1:run:headed
```

默认读取：

* `STAGE1_REQUEST_FILE=specs/stage1/stage1-request.community-create.example.json`

## 输出产物

* `t_runtime/stage1-results/<requestId>/<timestamp>/stage1-result.json`
* `t_runtime/stage1-results/<requestId>/<timestamp>/stage1-result.partial.json`
* `t_runtime/stage1-results/<requestId>/<timestamp>/evidence/`
* `t_runtime/stage1-results/<requestId>/<timestamp>/draft.acceptance-task.json`
* `t_runtime/stage1-results/<requestId>/<timestamp>/review-notes.md`

## 边界说明

本次仅完成第一段最小入口骨架，不包含：

* 第一段持久化写库接入
* 第一段复杂元素建模
* 第一段自动串行触发第二段
* 前端管理页面

## 验证说明

当前会话环境中 `node.exe / npm / npx` 存在启动异常，未在本会话内完成命令级执行验证。

---

@author Jiane
