# 运行目录与项目规范统一

## 实施日期

2026-03-11

## 变更内容

* 新增 `config/runtime-path.ts`，统一管理运行期目录配置
* `.env` 与 `.env.example` 新增运行目录相关环境变量
* `playwright.config.ts` 改为从环境变量读取 `outputDir` 和 HTML 报告目录
* `tests/fixture/fixture.ts` 接入 Midscene `setLogDir()`，统一 Midscene 输出目录
* `.gitignore` 增加 `t_` 前缀运行产物目录忽略规则
* `.github/workflows/playwright.yml` 同步更新 HTML 报告上传路径
* 新增项目级 `AGENTS.md`，统一命名、配置、日志、目录规范
* `README.md` 补充运行产物目录说明和新的 `.env` 示例

## 默认目录约定

* `t_test-results`
* `t_playwright-report`
* `t_midscene_run`

## 说明

* 运行期目录名已从硬编码改为环境变量驱动
* 后续新增自动生成目录时，需继续纳入 `.env` 和 `AGENTS.md` 约束
